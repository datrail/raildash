.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.db
*.db-wal
*.db-shm
*.egg-info/
build/

# `make stack` clones datrail/railmon here (see Makefile). Build input, not
# source: it is its own repository and must never be committed.
railmon/
